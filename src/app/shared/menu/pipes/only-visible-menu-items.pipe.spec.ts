import { OnlyVisibleMenuItemsPipe } from './only-visible-menu-items.pipe';

describe('OnlyVisibleMenuItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new OnlyVisibleMenuItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
